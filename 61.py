list=["casa", "carro", "dinheiro", "mulher"]
for lista in list:
    if lista=="casa":
        print(lista.upper())
    else:
        print(lista.title())
#o código contido no if só será exectado se e somente si a epressão lógica do if forverdadeira.
# o else é executado quando o if for falso.