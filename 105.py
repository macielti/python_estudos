languages={
    'samoel':'C',
    'wanderson':'PHP',
    'eutino':'Java',
    'bruno':'python',
}

for name in sorted(languages.values()):
    print(name.upper())
# a função values retorna apenas os valores das chaves do dicionário sem retornar o nome das Keys.
